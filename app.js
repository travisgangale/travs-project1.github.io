// All boxes with price and categories
//////////////////////////////////////

$(() => {
    for (let i = 0; i < 30; i++) {
        const $boxes = $('<div>');
        $boxes.addClass('square');
        $('#container').append($boxes);
    }

















































});
